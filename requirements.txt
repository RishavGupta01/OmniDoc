# ============================================================
# OmniWorkspace v2.0 — Dependency Manifest
# ============================================================
# The app auto-installs missing packages on demand via
# DependencyManager. This file documents the full dependency tree.
#
# Install everything at once:
#   pip install -r requirements.txt
#
# Or let the app handle it per-feature.

# === GUI ===
customtkinter>=5.2.0

# === Document Processing (Core) ===
PyMuPDF>=1.23.0
pypdf>=4.0.0
pdf2docx>=0.5.0
python-docx>=1.1.0      # DOCX read/write
python-pptx>=0.6.21     # PPTX read/write

# === Data Processing ===
pandas>=2.0.0
openpyxl>=3.1.0         # Excel support for pandas
lxml>=5.0.0             # XML/HTML parsing for pandas

# === Image Processing ===
Pillow>=10.0.0

# === Analysis (Optional - auto-installed on first use) ===
# easyocr>=1.7.0        # OCR (~1.5GB model download first run)
# sumy>=0.11.0          # Multi-algorithm summarization
# nltk>=3.8.0           # Tokenizer for sumy
# numpy>=1.24.0         # Required by easyocr

# === Windows Office Automation (Optional) ===
# comtypes>=1.3.0       # MS Office → PDF (requires Office installed)

# === Drag & Drop (Optional - auto-detected) ===
# tkinterdnd2>=0.3.0    # Native drag & drop support

# === Binary Dependencies ===
# Place in bin/ or add to PATH:
# - ffmpeg.exe   (media transcoding)
# - pandoc.exe   (document compilation)
# - LibreOffice  (Office→PDF fallback, install separately)
